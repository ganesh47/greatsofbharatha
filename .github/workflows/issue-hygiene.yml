name: Issue hygiene

on:
  issues:
    types: [opened, edited, labeled, reopened]

concurrency:
  group: issue-hygiene-${{ github.event.issue.number || github.run_id }}
  cancel-in-progress: true

permissions:
  issues: write

jobs:
  hygiene:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Require one status label at most
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          script: |
            const labels = context.payload.issue.labels.map(l => l.name)
            const status = labels.filter(l => l.startsWith('status/'))
            if (status.length <= 1) return
            core.setFailed(`Issue has multiple status labels: ${status.join(', ')}`)
